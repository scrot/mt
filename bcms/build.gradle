group 'org.uva.rdewildt.mt'
version '0.1'

dependencies {
    compile project(':xloc')
    compile project(':utils')

    compile 'com.google.code.findbugs:bcel-findbugs:6.0'
    testCompile group: 'junit', name: 'junit', version: '4.11'
}