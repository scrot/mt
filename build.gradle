group 'org.uva.rdewildt.mt'
version '0.1'

allprojects {
    apply plugin: 'java'
    apply plugin: 'idea'

    repositories {
        jcenter()
        maven { url 'https://repo.gradle.org/gradle/libs-releases-local' }
    }
}